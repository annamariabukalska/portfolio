{
   "name": "portfolio",
   "description": "Min portfolio",
   "version": "1.0.0",
   "type": "module",
   "scripts": {
      "scss": "node-sass style/style.scss -o style/",
      "open": "concurrently \"http-server -a localhost -p 1234\" \"open http://localhost:1234/\"",
      "start": "npm run scss && npm run open && -- --watch"
   },
   "dependencies": {
      "node-sass": "^9.0.0"
   },
   "author": "Anna Maria Bukalska",
   "devDependencies": {
      "concurrently": "^5.3.0",
      "http-server": "^0.12.3"
   }
}
